import { createApp } from 'vue'
// @ts-ignore: Vue SFC declaration missing in this project setup
import App from './App.vue'
import './style.css' // Make sure this is the only CSS file imported!

createApp(App).mount('#app')